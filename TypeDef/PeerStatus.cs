namespace Mirror.Punch
{
    /// <summary>
    /// Status of a peer
    /// </summary>
    public enum PeerStatus
    {
        OFFLINE = 0,
        CONNECTED,
        CONNECTING,
        DISCONNECTING,
    }
}
